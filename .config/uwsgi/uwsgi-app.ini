[uwsgi]
home = /root/.pyenv/versions/mulmul
chdir = /srv/mulmul/django_app
module = config.wsgi.deploy

socket = /tmp/eb.sock
chmod-socket = 666

enable-trheads = true
master = trueeb.s
vacuum = true
logger = file:/tmp/uwsgi.log

